Typescript
Angular
  -- introduction, folder structure
  -- components, nested components
  -- data binding(one way - string interpolation(c-->t), property binding(c-->t),event binding(t-->c), two way)